import React from 'react';
import {Button, TextInput, View} from 'react-native';
import {Formik} from 'formik';

const UserInput = props => (
  <View>
    <TextInput>User Input</TextInput>
  </View>
);

export default UserInput;
