import React from 'react';
import {Button, TextInput, View} from 'react-native';
import {Formik} from 'formik';

const UserOutput = props => <TextInput>User Output</TextInput>;

export default UserOutput;
